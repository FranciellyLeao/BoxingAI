name: boxing_ai
description: "Aplicativo Flutter de Boxe com IA e Visão Computacional On-Device (MediaPipe Pose)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  google_mlkit_pose_detection: ^0.10.0
  google_mlkit_commons: ^0.6.0
  vector_math: ^2.1.4
  flutter_riverpod: ^2.4.9
  shared_preferences: ^2.2.2
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
